# 打工人小账本｜项目总纲

> 范围说明：本文记录的是早期原型设想。当前产品方向以 `产品优化与迭代交接说明-v1.0.md` 内的 v1.2 为准；真实时薪、工作成本、自由基金安全垫和情景模拟已退出 V1 范围，后续开发不得继续据本文相关章节扩展。

> 状态：需求梳理中  
> 最后更新：2026-08-17

## 1. 产品定位

一个以「钱换算成时间」为核心视角的个人工作与财务工作台。帮助使用者快速记录日常消费、理解真实时薪、追踪财务安全垫，并用通勤、加班、涨薪和换城市等情景判断工作与生活的取舍。

产品面向单人使用，强调低摩擦、隐私优先和可解释性：任何金额、时间和结论都能追溯到明确的输入与公式。

## 2. 隐私与数据原则

### 2.1 本地优先

- 默认将流水、个人设置、附件和识别结果保存于当前设备的本地资料库，不要求注册或登录。
- 数据不主动上传、不使用第三方统计追踪；在未开启可选同步前，应用离线也可完整使用。
- 浏览器本地数据不能天然跨设备或跨浏览器。跨设备属于后续的**可选、端到端加密同步**能力：数据在本地加密后才可同步，服务端不应持有可读明文。
- 提供本地加密备份、导出（CSV / JSON）和导入恢复；第一次使用及关键操作应明确提示数据只存在本机，避免误以为已经云端保存。

### 2.2 写入与失败保护

- 新增、编辑、删除均在本地资料库中即时落盘，并给出明确的保存状态。
- 任何保存失败、浏览器存储空间不足、导入失败或识别失败，都显示原因、可执行的重试入口和不丢失的草稿；不静默吞掉操作。
- 页面先显示可操作的界面和本地骨架，再异步加载数据。读取失败时显示空状态/错误说明与重试、导入备份等选项，绝不白屏。
- 后续开启加密同步时，使用本地优先的离线队列和冲突提示；同步失败不影响本机已保存的数据。

## 3. 信息架构

1. **首页**：工作时间价值、最近 4 笔记录、月度概览、自由基金进度、存款曲线与快捷记账。
2. **记一笔**：10 秒手动记账；语音、图片、文字输入后的待确认提取结果。
3. **流水**：按时间浏览、筛选、搜索、编辑和删除所有记录。
4. **工作价值**：真实时薪计算器、公式展开、通勤/加班/涨薪/换城市情景模拟。
5. **自由基金**：目标、当前进度、安全垫、月度趋势与预计达成时间。
6. **设置**：时薪参数、分类、固定支出、自由基金、数据备份与隐私选项。

## 4. 核心功能与交互

### 4.1 真实时薪计算器

通过「综合到手收入 ÷ 为获得这份收入投入的总时间」估算真实时薪，并同时展示基本时薪与含工作成本后的净时薪。

默认输入：

- 综合到手月薪：工资、稳定奖金/补贴等可持续收入；年终奖等通过“发薪月数”摊入。
- 发薪月数：默认 12，可设为 13、14 或任意小数；用于把全年收入平摊至月度。
- 工作日与每日工作时间：可按月设定；缺省采用当地常规工作日的预估值并允许修改。
- 午休扣除：默认采用**预估模式**，无需每日打卡。可设置一个平均午休时长（建议初始为 1.25 小时，覆盖 11:30–13:00 或 11:30–13:30 的常见情况），需要更准确时再逐日修正。
- 单程通勤时间、通勤工作日：地铁通勤默认按耗时计入总投入时间。
- 平均每日无偿加班：默认计入；有偿加班应由使用者选择“计为额外收入”或“仅计时间”。
- 工作现金成本：见 4.2。

计算结果至少包含：

- 名义时薪：月度到手收入 ÷ 月度实际工作时长。
- 时间全成本时薪：月度到手收入 ÷（实际工作 + 通勤 + 无偿加班）时长。
- 净真实时薪：

  `（月度到手收入 - 月度工作现金成本） ÷（实际工作 + 通勤 + 无偿加班）时长`

- 每次消费相当于需工作多久，记账保存后即时换算为“约 X 小时 X 分钟”。

所有结果页均可展开查看代入数字后的完整公式、计算口径、未纳入项目和修改入口。

### 4.2 工作成本的科学口径

为避免把所有生活支出都错误算作“为了上班而发生的成本”，采用**增量成本**原则：只计入因为这份工作而比不工作/在家生活多花的钱。

- **默认纳入**：地铁/公交等通勤交通费、停车/过路费、为上班额外购买的餐食（例如外卖相对于带饭的差额）、工作必需的工装或工具。
- **可选纳入**：为住得离公司更近而多出的房租差额；仅填写“通勤房租差额”，而非整笔房租。
- **默认不纳入**：基础居住租金、日常做饭的基础食材、普通服装、个人娱乐和与工作无关的消费。
- **税和社保**：不作为“工作成本”重复扣除，因为它们已反映在“到手收入”里。可在设置中显示税前/到手差额作理解收入结构之用，但真实时薪默认只用到手收入。

该口径会在界面用简洁提示说明，避免造成“工作成本等于全部生活成本”的误解。

### 4.3 10 秒记账

必填：金额、分类；选填：备注、发生日期/时间、支付方式、是否固定支出。

- 保存后立即写入本地资料库，并在结果反馈中显示消费金额与工作时间等值。
- 支持预置分类和用户自定义分类；自定义分类可以选择图标和颜色。
- 支持“支出 / 收入 / 转账或不计入统计”三种类型，避免还信用卡、账户间转账重复记入收支。
- 首页展示最近 4 笔记录；流水页提供编辑、删除、搜索与筛选。删除必须二次确认或提供短暂撤销，避免误删。

建议的默认支出分类：餐饮、通勤、住房、日用、购物、娱乐、医疗健康、人情、学习成长、订阅服务、旅行、其他。默认收入分类：工资、奖金、报销、副业、礼金、其他。

### 4.4 自由输入与 Agent 记账规划

用户可以用文字、语音或图片随意表达，例如：“今天地铁 4 块，午饭 28”“这张小票记一下”“昨晚和朋友吃火锅 196”。

处理流程：

1. 本地解析可提取的金额、日期、类别、商户/备注和可能的拆分项。
2. 结果始终进入**待确认卡片**，清楚标出不确定字段、原始内容与修改入口。
3. 使用者确认后才写入正式流水；取消或解析失败时原始草稿保留，可重试或手动补录。
4. 默认不把语音、图片、文本发送到外部 AI 服务。若未来启用外部/云端识别，必须逐次明示授权，说明发送内容和用途；优先考虑本地模型或使用者自选的兼容模型服务。

第一版可先实现“结构化待确认工作流”，将智能识别接口与界面分离，后续再接入本地或已获授权的模型。

### 4.5 月度总结与存款曲线

- 显示本月收入、固定支出、弹性支出、结余和储蓄率。
- 固定/弹性支出以每条流水的标记为准，并允许在设置中为类别设定默认值；用户可随时覆盖。
- 首页使用原生 SVG 绘制累计结余/储蓄曲线，不依赖外部图表服务。应提供数据点悬浮提示、无记录空状态和无障碍文本描述。

### 4.6 自由基金与安全垫

- 自由基金目标初版为：目标覆盖月数 × 当前月度基础支出；同时支持直接填写一个目标金额。
- 安全垫默认以“必要固定支出 + 必要基础生活支出”计算，可选择 3 / 6 / 12 个月，并明确哪些类别被纳入。
- 展示当前可用存款、距目标差额、按照最近 3 个月平均结余的预计达成时间；预测应标明仅为估算，不作保证。

### 4.7 情景模拟

所有模拟均只改模拟值、不覆盖个人设置；可一键应用某个方案前预览差异。

- **通勤**：改变单程时间与交通费，对比月度投入时长、净真实时薪和年度时间成本。
- **加班**：改变每日无偿/有偿加班，展示真实时薪变化、月度额外时间与对应机会成本。
- **涨薪**：改变到手月薪、奖金或发薪月数，展示时薪、结余和自由基金达成时间的变化。
- **换城市**：对比城市/工作方案的收入、税后估算、房租增量、通勤、工作日、加班和日常生活成本；输入不足时应明确标注“基于用户估算”，不伪造精确度。

## 5. 资料库数据表（本地资料库）

### 5.1 流水记录 `transactions`

| 字段 | 说明 |
| --- | --- |
| `id` | 唯一标识 |
| `type` | 支出、收入、转账/不计入统计 |
| `amount` | 金额，统一使用最小货币单位避免精度误差 |
| `categoryId` | 分类标识 |
| `note` | 备注/商户 |
| `occurredAt` | 发生时间 |
| `isFixed` | 是否固定支出 |
| `paymentMethod` | 支付方式，可为空 |
| `source` | 手动、文字、语音、图片、导入 |
| `rawInputRef` | 原始输入的本地引用，可为空 |
| `createdAt` / `updatedAt` | 创建和更新时间 |
| `deletedAt` | 软删除/撤销支持，可为空 |

### 5.2 个人设置 `profile_settings`

单人本地资料库的键值型设置表，用于存放：

- 收入、发薪月数、工作日、每日工作时长、平均午休、通勤、加班与加班报酬口径。
- 通勤费、餐食增量、工作工具、通勤房租差额等工作成本参数。
- 自由基金目标金额/覆盖月数、安全垫月数、必要支出分类、可用存款基数。
- 默认分类、分类颜色/图标、固定支出默认规则。
- 隐私、备份、可选加密同步和外部 AI 授权状态。

### 5.3 辅助资料表（规划）

- `categories`：系统与自定义分类。
- `capture_drafts`：语音/图片/文字原文、提取结果、置信标识、确认状态和失败原因。
- `scenarios`：用户保存的通勤、加班、涨薪、换城市模拟方案。
- `sync_queue`：仅在使用者主动启用加密同步后使用。

## 6. 第一版边界

首版优先交付：本地资料库、个人参数、手动记账、最近记录、月度总结、真实时薪与公式、自由基金与原生 SVG 曲线、基础情景模拟、待确认录入框架、备份导入导出与失败状态。

首版不承诺：强制账户体系、默认云端同步、未经授权的第三方 AI 上传、自动银行流水连接、税务/投资建议或精确税后城市成本预测。

## 7. 待讨论事项

- 视觉方向、信息密度、移动端与桌面端的优先级。
- “当前可用存款”的录入方式，以及是否需要账户/现金余额分组。
- 固定支出的判断方式：手动标记、周期规则还是自动建议。
- 自由基金中哪些费用属于“必要基础生活支出”。
- 语音/图片识别未来采用本地模型、用户自带密钥，还是明确授权的外部服务。
- 可选端到端加密同步的实现和恢复密钥体验。
