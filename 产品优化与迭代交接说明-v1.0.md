# 打工人小账本｜产品优化与迭代交接说明 v1.3

> 文档状态：方案 1 已实现，可用于产品评审、Figma 补全、前端迭代与测试验收  
> 更新时间：2026-08-18  
> 适用对象：产品经理、交互/视觉设计师、前端工程师、测试人员  
> 相关基线：`打工人小账本-项目总纲.md`、`PRODUCT.md`、当前 PWA 实现；若范围冲突，以本文件 v1.2 为准
> 本次修订：采用“流水工作台”重设计，移除工作成本、自由基金安全垫、情景模拟和真实时薪；工资仅保留实际到账与简化到手估算；补充当前实现状态与 Figma 路线图

## 0. 一页结论

产品不应继续把“时间换金钱”作为重心。建议重新定位为：

**一款面向普通上班族的本地优先个人账本，用最低操作成本记录收入和支出，并自动生成清楚的周报、月报与到手工资估算。**

建设顺序：

1. 先保证账单记录准确、快速、可撤销。
2. 再完善账户、周期账单、预算、周报和月报。
3. 然后接入“税前工资 − 个人五险一金 − 个税 − 其他扣除”的简化到手估算。
4. 最后再考虑加密同步、快捷入口等便利能力。

关键产品决策：

- 工资收入采用双入口：默认直接记录银行卡实际到账；需要估算时，再输入税前工资和个人扣除，查看预计到手。
- 实际到账始终是账本事实。估算只用于参考，不能覆盖用户记录的收入。
- 五险一金优先允许用户直接填写个人实际扣除金额；城市化比例自动计算属于后续增强，不阻塞 V1。
- 首页默认回答“我这个月收了多少、花了多少、还剩多少、现在该做什么”，而不是一次展示全部功能。
- 桌面一级导航收敛为：`首页｜流水｜报表｜账户｜分类｜设置`，“记一笔”作为全局主操作；移动端使用 `首页｜记一笔｜流水｜报表｜设置`。
- 工作成本、自由基金安全垫、情景模拟和真实时薪从当前产品范围移除，不进入一级或二级导航。

### 0.1 当前实现状态（2026-08-18）

已完成：

- 方案 1“流水工作台”桌面首页与移动端自适应。
- 收入、支出、账户间转账；金额、分类、账户、时间、标签和备注。
- 本月收入、支出、结余、账户余额、累计趋势和总预算。
- 流水关键词、类型和账户筛选；编辑与删除。
- 每周、每月报表，包含收支汇总、趋势、支出分类和记录天数。
- 账户、分类、可选工资到手估算、本地 JSON 备份与恢复。
- 旧版真实时薪、工作成本、自由基金、安全垫和情景模拟从导航与页面中移除。

后续补齐：

- 退款、报销关联、余额校准、撤销删除和批量处理的完整数据口径。
- 周报/月报导出、隐私预览、周期账单和分类预算。
- 备份版本迁移、校验摘要、幂等导入和失败回滚。
- 多账本、多币种、附件和可选加密同步；这些能力保持二级层级，不增加首次记账门槛。

## 1. 产品目标与边界

### 1.1 目标用户

- 有固定工资，希望知道钱花到哪里、每月还能留下多少的上班族。
- 收入不完全固定，需要记录工资、奖金、副业或报销的人群。
- 不愿上传金融隐私，希望离线、本地保存数据的用户。
- 希望每周、每月快速看懂收入、支出和结余，但不愿维护复杂财务参数的用户。

### 1.2 核心使用场景

1. 支付后 10 秒内记下一笔支出。
2. 工资到账后直接记录实际收入；需要时再查看税前到到手的扣除估算。
3. 每周快速看一次支出结构与异常变化。
4. 每月确认收入、支出、净结余、储蓄率与预算执行情况。
5. 从流水追溯报表中的每个汇总金额。

### 1.3 第一阶段非目标

- 不直接连接银行账户或承诺自动同步所有支付平台。
- 不提供税务申报、投资建议或保证完全准确的到手工资计算。
- 不在账本基础不稳定时开发复杂 AI Agent、社交社区或游戏化系统。
- 不将单位承担的社保、公积金金额错误地从个人实发工资中扣除。
- 不开发真实时薪、工作成本、自由基金安全垫和情景模拟。
- 不要求用户建立薪资阶段、工时日志或复杂计薪规则才能记工资。

## 2. 建议的信息架构

```mermaid
flowchart TD
    A[首页] --> A1[本月收入 / 支出 / 净结余]
    A --> A2[快速记一笔]
    A --> A3[待处理提醒]
    B[记一笔] --> B1[支出]
    B --> B2[收入]
    B --> B3[转账 / 退款 / 报销]
    C[账本] --> C1[流水列表]
    C --> C2[账户与余额]
    C --> C3[筛选 / 搜索 / 批量处理]
    D[报表] --> D1[周报]
    D --> D2[月报]
    D --> D3[分类与趋势]
    D --> D4[工资扣除摘要]
    E[我的] --> E1[工资扣除模板，可选]
    E --> E2[分类 / 标签 / 周期账单]
    E --> E3[备份 / 恢复 / 同步]
    E --> E4[隐私与设置]
```

移动端建议固定底部五项导航。桌面端可以使用左侧导航，但名称、顺序和层级应与移动端一致。

## 3. 核心用户闭环

```mermaid
flowchart LR
    A[快速记账] --> B[确认分类与账户]
    B --> C[更新当月收支和余额]
    C --> D[周报发现变化]
    D --> E[月报调整预算或周期账单]
    E --> A
```

产品的留存来自“记录—反馈—调整”的循环，而不是来自功能数量。

## 4. 功能需求

### 4.1 账单记录基础能力

#### 4.1.1 交易类型

必须支持：

- 支出
- 收入
- 账户间转账
- 退款
- 报销：先记录垫付支出，报销到账后形成关联收入，报表可选择是否抵扣原支出
- 余额调整：只用于校准，不混入正常消费统计

后续支持：借入、借出、信用卡还款、分期。

#### 4.1.2 最小输入流程

移动端“记一笔”的默认顺序：

1. 输入金额。
2. 选择支出或收入；默认沿用上一次类型。
3. 选择分类；优先展示最近使用与高频分类。
4. 选择账户；默认上一次账户。
5. 保存。

备注、标签、商家、时间、附件、报销等放入“更多信息”，不要阻塞高频流程。保存按钮在金额输入后始终可见，金额键盘区域应支持单手操作。

#### 4.1.3 快捷输入

- 支持自然语言，例如“午饭 32 微信”“工资 12500 招商银行”。
- 解析结果先形成可编辑草稿，不直接写入正式流水。
- 时间、日期和金额必须按语义识别，不能把 `12:30` 的数字误识别为金额。
- 支持复制上一笔、常用模板、周期账单一键确认。
- 桌面端可提供快捷键；移动端后续可接系统快捷指令或分享菜单。

#### 4.1.4 流水字段

每笔交易至少包含：

- `id`
- `type`
- `amount`
- `currency`
- `occurred_at`
- `category_id`
- `account_id`
- `merchant`
- `note`
- `tag_ids`
- `related_transaction_id`
- `reimbursement_status`
- `source`
- `created_at` / `updated_at`
- `deleted_at`

金额应使用整数分或十进制定点数保存，禁止直接依赖 JavaScript 浮点数累计金额。

#### 4.1.5 账本查询与管理

- 按日期范围、类型、分类、账户、标签、金额区间筛选。
- 搜索框必须有可见标签或明确的辅助文本，不能只依赖 placeholder。
- 支持按天、周、月分组。
- 支持编辑、复制、软删除和撤销。
- 支持批量改分类、账户、标签和删除。
- 长列表采用分页或虚拟列表，避免每次输入搜索词都重绘全部页面。

### 4.2 账户与余额

账户类型建议包含：现金、银行卡、信用卡、支付平台、储蓄账户、其他。

要求：

- 转账不计入收入或支出。
- 信用卡消费发生时计支出，还款时只做账户转账。
- 首页总资产与月度净结余分开显示，避免概念混淆。
- 允许设置账户是否计入总资产、是否归档。
- 余额校准必须保留调整记录与原因。

### 4.3 工资收入与到手估算（简化版）

本模块只解决两个问题：用户实际收到了多少钱；如果用户输入的是税前工资，扣除个人五险一金、个税和其他扣款后，预计能到账多少。它不是薪酬管理系统，也不要求用户维护工时、长期薪资规则或职业阶段。

#### 4.3.1 直接记实际到账（默认）

工资到账后，默认只要求填写：

- 实际到账金额
- 到账日期
- 入账账户
- 收入分类，默认“工资”

工资月份、公司、备注和工资条附件全部放在“更多信息”中。保存后立即进入账户余额、本月收入和报表。

试用期 80%、月中入职只发半个月、请假扣薪、奖金波动或补发工资，都不需要额外模式。用户按银行卡实际到账金额记录这一笔即可，不必修改长期月薪或解释计算过程。

#### 4.3.2 从税前工资估算到手（可选）

用户选择“从税前估算”后，页面只展示一张简化表单：

- 税前应发工资
- 个人五险扣除合计
- 个人住房公积金
- 个人所得税
- 其他扣除，可选
- 税后补贴，可选

系统实时显示：

```text
预计到手 = 税前应发
          - 个人五险扣除
          - 个人住房公积金
          - 个人所得税
          - 其他扣除
          + 税后补贴
```

V1 优先支持用户直接填写工资条中的个人扣除金额，避免首次使用就要求选择城市、缴费基数、比例、专项附加扣除和累计税额。后续若增加城市规则模板，也只负责自动填入建议值，用户仍可修改，且结果始终标注“预计”。

估算结果提供“按预计金额记入账本”，但在工资真正到账后，应允许用户一键改为实际金额。实际到账值覆盖报表中的估算值，历史估算作为说明保留，不能额外生成第二笔收入。

#### 4.3.3 收入统计口径

- 工资、奖金、兼职和副业收入计入收入。
- 账户间转入不计入收入。
- 退款与报销单独展示，默认抵扣相关支出，不混入工资收入。
- 报表以已确认的实际流水为准；尚未到账的预计工资不计入本月实际收入。

### 4.4 周报

默认周期支持周一至周日，并允许用户修改一周开始日。

周报模块：

- 本周收入、支出、净结余。
- 与上周相比的金额和百分比变化。
- 分类排行与变化最大的分类。
- 最大三笔支出，点击可追溯到原流水。
- 未分类、疑似重复、待报销账单提醒。
- 一条中性、可执行的复盘建议，不使用羞辱或恐吓文案。

周报状态：

- 数据完整：展示全部模块。
- 数据不足：说明统计基于多少笔数据，不生成夸张结论。
- 无交易：引导记第一笔或检查筛选范围。
- 数据异常：列出待确认项，不静默修正。

### 4.5 月报

月报必须回答：钱从哪里来、花到哪里、最终留下多少、与上月相比发生了什么。

核心指标：

- 总收入、工资收入、其他收入。
- 总支出与分类占比。
- 净结余与储蓄率。
- 账户余额变化与现金流一致性校验。
- 分类预算执行情况。
- 工资收入与可选的个人扣除摘要。
- 分类趋势、周期账单、异常大额。

导出：

- PNG：适合保存或分享，默认隐藏账户名称和备注。
- PDF：完整月报。
- CSV：原始统计明细，便于二次分析。

导出前提供隐私预览，可选择隐藏金额、商家、账户、备注和附件。

### 4.6 预算、周期账单与提醒

- 支持总预算与分类预算。
- 预算周期支持自然月、发薪日至下个发薪日前一天。
- 周期账单支持固定金额与浮动金额。
- 周期账单到期时先生成待确认项，不默认伪造正式交易。
- 提醒可以关闭；本地 PWA 无权限时必须有页面内待办替代方案。
- 预算超支提示使用事实与趋势，不使用负面人格判断。

### 4.7 暂不进入当前版本的功能

以下能力会显著增加配置和理解成本，且不是日常记账的必要条件，当前版本直接移除，不在首页、导航或报表中露出：

- 真实时薪与消费工时换算
- 工作成本口径，包括通勤、工作餐增量、工装和通勤房租差额
- 自由基金、安全垫月数与覆盖月数
- 通勤、加班、涨薪等情景模拟
- 工时日志、薪资阶段与复杂计薪规则

未来只有在核心记账留存稳定、用户研究证明存在明确需求时，才重新评估这些功能；不得因为代码已存在就默认保留。

### 4.8 隐私、备份与同步

- 默认本地优先，明确展示数据存放位置。
- 备份文件包含 `schema_version`、生成时间、数据范围和校验值。
- 恢复前先校验版本和内容，并展示将新增、覆盖或跳过的数据数量。
- 恢复过程使用单一事务或可回滚策略，避免部分导入。
- 备份可选密码加密；未加密时必须明确提示文件包含敏感财务信息。
- 若后续增加同步，应采用用户可选择的端到端加密，并允许随时导出和退出。

## 5. 首页与关键页面体验

### 5.1 首页

首屏优先级：

1. 本月收入、支出、净结余。
2. 主按钮“记一笔”。
3. 待确认的周期账单、草稿或报销。
4. 最近流水。
5. 一条来自周报/月报的洞察。

首页不展示真实时薪、自由基金、安全垫或情景模拟入口。

### 5.2 记一笔

- 移动端先金额、后分类。
- 手动输入与自然语言不要永久并列成两个同等复杂表单；可使用模式切换或把自然语言作为默认入口之一。
- 保存后显示轻量确认，并提供短时撤销。
- 日期时间使用用户本地时区；编辑后不得发生二次偏移。

### 5.3 可用性与无障碍

- 主要文本与按钮满足 WCAG AA 对比度目标。
- 交互目标建议至少 44×44 CSS 像素。
- 所有表单有可见标签、错误说明和键盘焦点状态。
- 不仅依赖颜色表达收入、支出、风险或成功。
- 支持系统减少动态效果设置。

## 6. 数据模型建议

建议在现有 `transactions` 与设置数据上扩展：

| 数据表 | 用途 | 关键字段 |
| --- | --- | --- |
| `accounts` | 账户与余额 | type、name、opening_balance、include_in_assets、archived_at |
| `categories` | 收支分类 | type、parent_id、icon、color、sort_order |
| `transactions` | 正式流水 | type、amount_minor、account_id、category_id、occurred_at、deleted_at |
| `transaction_links` | 转账、退款、报销关联 | source_id、target_id、relation_type |
| `recurring_rules` | 周期账单 | cadence、next_due_at、amount_mode、status |
| `budgets` | 预算 | period_type、category_id、amount_minor、start_day |
| `salary_estimates` | 税前到到手的可选估算 | income_transaction_id、pay_month、gross、social_insurance、housing_fund、individual_tax、other_deductions、net_estimate、actual_net |
| `deduction_templates` | 用户保存的个人扣除模板 | name、social_insurance、housing_fund、other_deductions、updated_at |
| `report_snapshots` | 可复现的周/月报 | period、metrics、rule_version、generated_at |
| `tags` | 自定义标签 | name、color |
| `attachments` | 本地附件索引 | transaction_id、mime_type、checksum、storage_ref |
| `undo_log` | 短时撤销 | action_type、payload、expires_at |

所有表必须考虑：版本迁移、唯一约束、幂等导入、软删除与备份兼容。

## 7. 当前实现的技术整改

### P0：发布前必须处理

1. 日期时间不能使用 `toISOString()` 直接填充本地 `datetime-local`；当前实现会产生时区偏移。
2. 草稿确认和备份导入需要使用 IndexedDB 事务，防止部分成功后重试造成重复。
3. 夜间主题主按钮的白字与绿色背景对比度不足，需要调整前景色或背景色。
4. 删除、恢复、批量导入提供预览、撤销或回滚。
5. 为金额、税费、工资、报表统计建立确定的精度与舍入规则。
6. 删除 `profile.monthlyNet × payMonths ÷ 12` 对收入记录和报表的影响；实际收入只以流水事实为准。
7. 收入类型下隐藏“固定支出”，并把报销、退款、转入与劳动收入分开统计。
8. 从导航、首页、报表和设置中移除真实时薪、工作成本、自由基金安全垫与情景模拟入口。

### P1：核心功能阶段处理

1. 将目前集中在 `app.js` 的页面、数据访问、计算和状态逻辑拆分为模块。
2. 页面更新改为按当前路由和局部状态渲染，搜索输入增加防抖或局部更新。
3. 建立数据库迁移层和 `schema_version`。
4. 自然语言解析器区分金额、时间、日期、数量和账户尾号。
5. 大量流水使用索引、分页或虚拟列表。
6. 为工资收入增加实际/估算状态和可选扣除明细。
7. 建立统一的收入统计服务，确保转账、退款和报销不会污染收入统计。

### 自动化测试最低范围

- 香港、上海、北京等不同时区或夏令时环境下的新增和编辑时间。
- 收入、支出、转账、退款、报销对月报的影响。
- 金额精度、舍入和负数边界。
- 税前工资减去个人五险、公积金、个税和其他扣除后的估算公式与舍入。
- 半月工资、试用期工资、奖金波动和补发工资均可直接作为普通收入流水记录。
- 报销、退款、转入不能进入工资收入统计。
- 草稿确认、批量导入和失败重试的幂等性。
- 旧版本备份迁移、新版本备份校验、损坏文件拒绝导入。
- 空数据、部分数据、异常数据下的周报和月报。
- 360px 宽移动端的记账流程、键盘遮挡、触控尺寸和焦点顺序。

## 8. 分阶段路线图

### Phase 0：正确性与基础体验（1–2 周）

- 修复本地时间、事务、金额精度、对比度与触控问题。
- 删除真实时薪、工作成本、自由基金安全垫和情景模拟入口，修正收入分类和“固定支出”字段。
- 增加撤销、备份版本、恢复预检。
- 建立核心单元测试与最小端到端测试。

完成标准：现有功能的数据不会因常见操作产生时间错位、重复写入或部分导入。

### Phase 1：账本主链路（3–6 周）

- 重构五项信息架构。
- 完成交易类型、账户、筛选、批量处理、周期账单、预算。
- 完成实际收入流水、工资分类和转账/退款/报销统计口径。
- 上线周报、月报及导出。
- 首页改为收支与待办导向。

完成标准：新用户可以在首次使用 60 秒内完成第一笔账；连续记录后能看懂一份可信月报。

### Phase 2：简化到手工资（2–4 周）

- 上线“直接记实际到账”和“从税前估算”两个入口。
- 支持个人五险、公积金、个税和其他扣除金额。
- 支持保存个人扣除模板，减少下月重复输入。
- 完成预计到账与实际到账的单笔对账，不重复生成收入。
- 城市规则自动填充仅作为后续可选增强，不进入本阶段完成标准。

完成标准：用户不阅读说明也能在一分钟内完成工资记录，并清楚区分预计到账和实际到账。

### Phase 3：跨端与增长能力（后续）

- 可选端到端加密同步。
- 系统快捷入口、通知和分享菜单。
- 更丰富但默认脱敏的报表分享。

## 8.1 Figma 路线图

Figma 不再继续扩展旧版时薪、自由基金或模拟页面。以已选方案 1 为唯一视觉基线，建议按以下页面与顺序整理：

### Figma 页面结构

1. **00 Foundations**
   - 颜色变量：Background、Surface、Ink、Line、Primary、Income、Expense。
   - 字体样式：Page title、Section title、Body、Label、Amount。
   - 间距、圆角、焦点环、语义状态和 Phosphor Regular 图标规范。
2. **01 Components**
   - Desktop sidebar、Mobile bottom navigation、Page header、Month switcher。
   - Button、Icon button、Text link、Input、Select、Segment control、Dialog、Toast。
   - Summary metric、Transaction row、Account row、Category item、Budget progress、Chart legend。
   - 每个交互组件补齐 default、hover、focus、active、disabled、error 状态。
3. **02 Desktop 1536×1024**
   - D01 首页｜流水工作台（已选视觉基线）。
   - D02 记一笔对话框｜支出、收入、转账。
   - D03 流水｜默认、搜索结果、筛选后、空状态。
   - D04 报表｜周报、月报、空数据。
   - D05 账户｜列表、新增、编辑。
   - D06 分类｜收入、支出、新增。
   - D07 设置｜账本、工资到手估算、备份和危险操作。
4. **03 Mobile 375×812**
   - M01 首页。
   - M02 记一笔｜金额优先、键盘打开、保存成功。
   - M03 流水与筛选。
   - M04 周报与月报。
   - M05 设置与工资估算。
5. **04 States & Prototype**
   - Loading、empty、save success、read/write error、import preview、delete confirmation。
   - 原型主链路：`首页 → 记一笔 → 保存 → 首页更新 → 流水搜索 → 周报/月报`。

### Figma 标注要求

- 桌面基准画布 `1536 × 1024`，侧栏 `236px`；移动端基准 `375 × 812`。
- Auto Layout 和变量优先，避免为每个页面复制独立样式。
- 金额使用 tabular numerals；收入和支出除颜色外必须保留文字或正负号。
- 首页与实现对齐：收入、支出、结余、最近流水、账户、趋势、总预算，不新增功能卡。
- Figma 中删除或归档旧版真实时薪、工作成本、自由基金、安全垫和情景模拟 Frame，防止继续被开发引用。

## 9. 关键验收标准

### 9.1 记一笔

- 移动端从打开页面到保存常用支出不超过 5 个必要动作。
- 保存后首页、账户余额、账本和当前报表结果一致。
- 误操作可在短时间内撤销。
- 编辑交易时间前后保持用户所见的本地时间。

### 9.2 到手工资

- 用户可只填实际到账金额、日期和账户，不被迫配置工资、工时或扣除参数。
- 月中入职、试用期或奖金波动直接记录当月实际金额，不需要建立特殊模式。
- 用户选择“从税前估算”后，能直接看到税前工资减去个人五险、公积金、个税和其他扣除的过程。
- 估算结果明确标注“预计”，且不会在到账前计入本月实际收入。
- 单位承担项不从个人实发金额重复扣除。
- 用户录入实际到账后，可看到估算差额但不会自动覆盖实际值。
- 报销、退款和账户转入不计入工资收入。

### 9.3 周报/月报

- 每个汇总金额均可点击追溯到构成流水。
- 转账不计入收支；退款、报销按用户选择的统计口径处理。
- 报表明确展示统计周期、数据笔数和生成时间。
- 数据不足时不生成误导性趋势。
- 导出前可预览脱敏项。

### 9.4 备份恢复

- 损坏或不兼容文件不能写入正式数据。
- 恢复前显示影响范围，恢复失败不会留下部分数据。
- 同一备份重复导入不会重复创建相同记录。

## 10. 衡量指标

建议只采集经用户同意的匿名产品指标；本地优先版本也可以仅在设备内计算。

- 激活率：首次打开后 24 小时内完成第一笔账的比例。
- 核心激活：完成第一笔账、设置至少一个账户并查看首份周报。
- 记账效率：常用交易的中位完成时间和必要动作数。
- 7 日、30 日仍有有效记账行为的用户比例。
- 每周复盘打开率、月报完成率、报表导出率。
- 未分类交易比例、重复交易率、撤销率。
- 工资记录完成时间、预计到账转实际到账的完成率。
- 备份成功率、恢复成功率和最近备份距今天数。

## 11. 交接分工

### 产品

- 明确交易、退款、报销、转账、预算的统计口径。
- 确认 V1 使用手填个人扣除金额，不承诺城市规则自动计算。
- 为每个阶段建立范围、验收标准和不做事项。

### 设计

- 先设计移动端金额优先的记账流程，再适配桌面端。
- 补齐空、加载、错误、部分数据和危险操作状态。
- 设计报表追溯、隐私预览，以及“实际到账 / 从税前估算”的双入口。
- 建立颜色、字体、间距、圆角、表单、按钮和图表规范。

### 前端与数据层

- 优先完成 P0 数据正确性整改。
- 拆分页面、状态、数据访问、统计和简化工资估算模块。
- 建立数据库版本迁移、事务和幂等导入。
- 报表使用统一统计服务，避免页面各自重复计算。

### 测试

- 按第 7 节建立自动化覆盖。
- 对简化工资公式、预计值与实际值切换使用固定样例核验。
- 对移动端键盘、离线、安装 PWA、恢复备份和大数据量做专项测试。

## 12. 审计证据索引

| 顺序 | 文件 | 主要观察 |
| --- | --- | --- |
| 1 | `audit-evidence/01-desktop-home.png` | 首页信息完整，但工资与基金抢占首屏，今日收支反馈不足 |
| 2 | `audit-evidence/02-desktop-capture.png` | 手动表单和智能输入并列，认知负担较高；时间存在时区偏移 |
| 3 | `audit-evidence/03-desktop-wage.png` | 首次配置成本过高；改为“直接记实际到账 / 从税前估算”两个入口 |
| 4 | `audit-evidence/04-desktop-simulator.png` | 不属于简单日常记账主任务，V1 删除情景模拟 |
| 5 | `audit-evidence/05-desktop-settings.png` | 本地隐私表达较好，但备份未加密、恢复缺少预检与回滚 |
| 6 | `audit-evidence/06-mobile-home.png` | 移动端总体清楚，但部分分类和操作目标偏小 |
| 7 | `audit-evidence/07-mobile-capture.png` | 最急需优化：10 秒目标与完整表单优先的结构冲突 |
| 8 | `audit-evidence/08-desktop-ledger.png` | 空状态友好，但筛选、账户、分页和批量处理不足 |
| 9 | `audit-evidence/09-desktop-freedom.png` | 自由基金与安全垫月数理解成本高，V1 删除 |

## 13. 评审时必须先确认的问题

1. 报销在默认报表中采用“抵扣支出”还是“收入 + 支出同时保留”？
2. 预算周期默认自然月还是发薪周期？
3. 账户余额是否由用户手动维护，何时允许余额校准？
4. 附件是否只保存本地，备份时是否默认包含？
5. 是否在 Phase 1 就支持信用卡，还是先只支持普通账户？
6. 报表导出是否需要品牌水印，以及默认隐藏哪些隐私字段？

---

本文件是产品与实现交接基线。任何新增功能都应先说明它如何强化“快速记账—可信统计—周期复盘”的核心循环，再进入开发。
