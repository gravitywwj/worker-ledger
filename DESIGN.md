---
name: 打工人小账本
description: 桌面优先、轻量清楚的本地个人账本。
colors:
  background: "#ffffff"
  sidebar: "#fbfcfb"
  surface-subtle: "#f7f9f7"
  surface-tint: "#edf5f0"
  ink: "#15231d"
  ink-soft: "#536159"
  line: "#e1e7e3"
  primary: "#0c6b3e"
  income: "#13865a"
  expense: "#de4d49"
typography:
  fontFamily: "Inter, ui-sans-serif, system-ui, Segoe UI, PingFang SC, Microsoft YaHei, sans-serif"
  pageTitle: "25px / 760 / 1.2"
  sectionTitle: "16px / 720 / 1.4"
  body: "14px / 500 / 1.5"
  label: "12px / 650 / 1.5"
rounded:
  control: "8px"
  panel: "12px"
spacing:
  page: "30px 36px 56px"
  panel: "18px"
  field: "15px"
icons:
  library: "Phosphor Icons Regular"
---

# Design System: 打工人小账本

## 设计方向

当前视觉基线是已选方案 1“流水工作台”。它参考 ezBookkeeping 的克制侧栏、密集但清楚的流水列表和轻量统计，同时保留产品原有的绿色身份。

设计服务于记账任务，不追求品牌展示。页面应像一个长期使用的个人工具：熟悉、安静、可信，打开后第一眼看到本月收支，第二眼看到最近流水，第三眼才看到趋势、预算或设置。

## 桌面结构

- 设计基准画布：`1536 × 1024`。
- 侧栏：`236px`，品牌置顶，六项导航，底部显示本地保存状态。
- 主内容：左右 `36px` 页面边距，最大内容宽度 `1320px`。
- 首页摘要：左侧收入与支出双列，右侧结余；面板高度约 `164px`。
- 首页工作区：左侧约 70% 为分日期流水，右侧约 30% 为账户、趋势和预算。
- 页面不使用全局顶栏；月份切换、搜索和“记一笔”在内容区顶部完成。

## 移动结构

- 基准画布：`375 × 812`。
- 侧栏改为底部五项导航：`首页｜记一笔｜流水｜报表｜设置`。
- 汇总改为纵向；流水表改为金额右对齐的列表行。
- 移动端主操作保持至少 `44px` 触控面积；底部导航包含安全区。

## 颜色

- **背景**：纯白，不使用奶油色、渐变或纹理。
- **主行动绿**：只用于主要按钮、当前导航、收入趋势和保存状态。
- **支出红**：只表达支出金额和必要警示；文字标签同时写明“支出”，不能只依靠颜色。
- **浅绿表面**：当前导航、收入标签和轻量选中状态。
- **边框**：使用低对比度细线分组，静止面板不加宽大阴影。

## 字体与数字

全产品使用一套系统无衬线字体。金额使用 tabular numerals；页面标题不超过 `25px`，摘要金额约 `32px`。中文正文保持 `14px`，辅助信息不低于 `11px`，并保证 WCAG AA 对比度。

## 组件

### 导航

- 当前项：浅绿背景、绿色图标和文字。
- 普通项：深灰文字，悬停时仅改变浅背景和文字色。
- 图标统一使用 Phosphor Regular，不使用 emoji、文字符号或自绘 SVG。

### 按钮

- 主按钮：绿色实底、白字、`8px` 圆角、高度 `40px`。
- 次按钮：白底细边框。
- 文字按钮：用于“全部流水”“管理账户”“设置预算”等二级动作。
- 所有按钮必须有 hover、focus、active 和 disabled 状态。

### 面板

- 默认白底、`1px` 边框、`12px` 圆角，无静态投影。
- 面板标题高度约 `58px`，内容区使用 `18–20px` 内边距。
- 避免面板内再次嵌套完整卡片；数据行用分隔线组织。

### 流水

- 桌面按日期分组，列为时间、类型、分类、账户、备注、金额。
- 收入和支出同时使用文字标签与语义色。
- 金额右对齐；编辑和删除只在完整流水页出现。
- 空状态需要解释第一步并提供“记录第一笔”。

### 表单与对话框

- 输入高度 `42px`，边框 `1px`，圆角 `8px`。
- 记账类型使用支出、收入、转账三段选择。
- 默认只显示金额、分类、账户、时间、标签和备注；工资估算不进入记账必填流程。
- 对话框只用于新增和编辑，筛选与报表切换保留在页面内。

### 图表

- 首页只显示累计收入和支出两条趋势线。
- 报表使用按日柱状图和分类条，不做装饰性面积图或 3D 图。
- 图表必须配文字图例和可读数字，数据为空时改用明确空状态。

## 状态与动效

- 普通交互 `150–180ms`，只表达悬停、选择、保存和对话框状态。
- 不使用页面入场编排、弹跳或装饰性运动。
- `prefers-reduced-motion: reduce` 下关闭非必要动画。
- 读取失败保留页面壳和重试入口；保存成功使用短暂通知。

## 禁止项

- 真实时薪、工作成本、自由基金、安全垫或情景模拟入口。
- 毛玻璃、渐变文字、超过 `16px` 的面板圆角、宽大软阴影。
- 满屏同尺寸卡片、巨大英雄数字、装饰性插画和游戏化连续打卡。
- 用自绘图形、emoji 或 Unicode 符号替代标准图标。
