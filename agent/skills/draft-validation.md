# Skill: draft-validation

用于在草稿展示前发现模型遗漏和幻觉。

校验内容：

- 草稿数量与脚本候选金额数量一致。
- 金额按分比较，支持重复金额，不因浮点误差误判。
- 每笔都有合法类型、分类、账户和 ISO 时间。
- 退款/退回的类型与语义一致。
- 转账必须有转入账户且不能伪装成收入或支出。

校验失败时把具体错误作为工具观察结果返给模型，只允许模型再修正一次；前端仍然只展示可编辑草稿，不直接入账。
